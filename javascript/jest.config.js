module.exports = {
  automock: false,
  bail: false,
  moduleFileExtensions: ['js'],
  roots: ['<rootDir>/tests'],
  testEnvironment: 'node',
  testMatch: ['**/?(*.)+(spec|test).js?(x)'],
  verbose: true,
}
